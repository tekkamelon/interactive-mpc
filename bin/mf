#!/bin/sh

set -eu
# ====== 変数の設定 ======

export LC_ALL=C
export LANG=C

export POSIXLY_CORRECT=1
# ====== 変数の設定ここまで ======

mpc playlist | grep -n . | fzf --reverse | cut -d":" -f1 | xargs mpc play
 
